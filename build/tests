#!/bin/bash

set -euxo pipefail

./build/docker-build -t local-registry/go-links_test --env test

docker-compose -f docker-compose.tests.yml up -d
set +e
docker-compose -f docker-compose.tests.yml run --entrypoint bin/run-tests tests
rc=$?
docker-compose -f docker-compose.tests.yml down
exit $rc
