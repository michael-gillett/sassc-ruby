export default function UiSubHeader(props) {
  // User provides:
  //  - a title for the header (title) : view
  
  const { title } = props;
  
  return (
    <div className="lr-ui-sub-header">
      <div className="lr-ui-sub-header-title">{title}</div>
    </div>
  );
};
