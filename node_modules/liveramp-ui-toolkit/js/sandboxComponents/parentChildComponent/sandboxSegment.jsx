import numeral from 'numeral';

const SandboxSegment = React.createClass({
  render() {
    const segment = this.props.model;

    return (
      <div className="row">
        <div className="col-xs-11 parent-child-padding-swap-col-6">
          <div className="field-name-wrapper">
            <span className={"field-name-container " + segment.type}>
              <span id={segment.id} className="field-name">
                {segment.name}
              </span>
            </span>
          </div>
        </div>
        <div className="col-xs-1">
          <span className="view btn btn-default btn-xs" onClick={this.props.openDetailView.bind(null, segment.id)}>
            More
          </span>
        </div>
      </div>
    );
  }
});

export default SandboxSegment;
