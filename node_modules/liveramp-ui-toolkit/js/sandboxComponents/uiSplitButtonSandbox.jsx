import UiSplitButton from '../components/uiSplitButton.jsx';

const selectOptions = [
  'Soup',
  {
    text: 'Tea',
  },
  {
    text: 'MargaritasMargaritasMargaritas',
    value: 2,
    handleChange: (text, value) => {
      console.log(text + value.toString())
    }
  }
];

const buttonFunction = (e) => {
  e.preventDefault;
  console.log('ouch!')
};

const sandBoxStyle = {
  margin: '100px'
}

const UiSplitButtonSandbox = React.createClass({
  render () {
    const options = ['a', 'b', 'c'];
    return (
      <div style={sandBoxStyle}>
        <div className="lr-ui-table-column-selector-container">
          <UiSplitButton
            label={'Coffee'}
            dropDownOptions={selectOptions}
            handleButtonClick={buttonFunction}
            handleChangeDefault={() => {console.log('default function')}}
          />
        </div>
        <div>
          Some dummy content below the split button component
        </div>
      </div>
    )
  }
});

export default UiSplitButtonSandbox;
