import UiDropdown from '../components/uiDropdown.jsx';
import UiIcon from '../components/uiIcon.jsx';

const UiDropdownSandbox = React.createClass({
  render () {
    const options = ['a', 'b', 'c'];
    return (
      <div className="lr-ui-table-column-selector-container">
        <UiDropdown
          title={<UiIcon icon='admin' dimensions={[20,20]} />}
          options={options}
          handleChangeDefault={() => {console.log("Change");}}
          hideCaret={true}
          />
      </div>
    )
  }
});

export default UiDropdownSandbox;
