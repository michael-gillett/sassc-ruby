<% unless config['target'] == 'local' %>
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: go-links-ingress
  namespace: default
spec:
  rules:
  - host: this-is-not.go-links.liveramp.net
    http:
      paths:
      - backend:
          serviceName: go-links
          servicePort: 80
<% end %>

