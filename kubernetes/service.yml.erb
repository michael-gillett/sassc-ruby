---
apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: go-links
  labels:
    app: go-links
spec:
  type: NodePort
  selector:
    app: go-links
  ports:
  - protocol: TCP
    port: 80
    targetPort: app-server-port
